create table PCPEDC (
  NUMPED INTEGER NOT null, 
  DATA DATE,
  CODCLI INTEGER,
  CODUSUR INTEGER,
  DTENTREGA DATE,
  CODFILIAL INTEGER,
  TOTPESO NUMERIC(15,6),
  TOTVOLUME NUMERIC(15,6),
  VALOR_TOTAL_LIQ NUMERIC(15,2),
  HORA INTEGER,
  MINUTO INTEGER,
  NUMSEQENTREGA INTEGER,
  NUMNOTA INTEGER,
  DTFAT DATE,
  POSICAO CHAR(1)
);